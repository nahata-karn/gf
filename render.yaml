services:
  - type: web
    name: goodfire-feature-explorer
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m gunicorn goodfire_webapp.app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0 